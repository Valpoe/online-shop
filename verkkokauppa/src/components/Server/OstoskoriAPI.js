/// Tietokannan ostoskori taulun hyödyntämistä.

/// Kopio test appista, ei toimi tässä projektissa

/*
let ostoskoriTuotteet =[];

const LisääOstoskoriin = {

    AddTuote(tuote){
        /// Tarkastaa onko samalla id oleva tuote korissa
        let samatuote = ostoskoriTuotteet.findIndex((item) => item.id == tuote.id);
        /// Jos samaa tuotetta on korissa, poistetaan
        if(samatuote !==-1){
            ostoskoriTuotteet[samatuote].quantity++;
        }
        /// jos ei samoja, lisätään
        else{
            ostoskoriTuotteet.push({...product, quantity: 1})
        }
        return Promise.resolve(ostoskoriTuotteet);
    }
};

export default LisääOstoskoriin;

*/